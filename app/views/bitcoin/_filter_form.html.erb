<%= form_for(@filterForm, url: root_path, method: :get) do |f| %>
  <%= f.date_field :start_date, value: @filterForm.start_date %>
  <%= f.date_field :end_date, value: @filterForm.end_date %>
  <%= f.select :currency, Bitcoin::FilterForm.available_currencies %>
  <%= f.submit 'submit' %>
<% end %>
